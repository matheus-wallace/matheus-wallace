import React from "react";
import { MenuSubtitle } from "./styles";

const SubtitleMenuMobile = ({ title }: { title: string }) => {
  return <MenuSubtitle>{title}</MenuSubtitle>;
};

export default SubtitleMenuMobile;
